# Environment Configuration
ENV=production
DEBUG=False
HOST=0.0.0.0
PORT=8080

# For production, update CORS_ORIGINS in config.py with your actual domain
# CORS_ORIGINS will be automatically extended for production environment